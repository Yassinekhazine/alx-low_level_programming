#include "main.h"

/**
 * _strlen - returning the string's length
 * @str: the string to get the length of.
 * Return: The length of string
 */

int _strlen(char *s)
{

size_t length = 0;
while (*str++)
length++;
return (length);
}
